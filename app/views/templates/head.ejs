<meta charset="UTF-8">
<meta name="theme-color" content="#FA8072">
<title><%# = pageTitle %></title>
<script src="/js/socket.io-1.3.5.js"></script>
<script src="/js/jquery-2.1.4.min.js"></script>
<script>
    var socket = io.connect('http://<%=host%><%=port%>');
    socket.on('news', function () {
        <% if (login) { %>
        socket.emit('clientGetNotices',{
            userId: <%=user.id%>
        });	// 로그를 가져옴
        <% } %>
        
        socket.on('serverGetNotices', function(data){
            for( var i in data){
                if( window.confirm( "알림!: " + data[i].msg ) == true ){
                    socket.emit('clientRemoveNotice',{
                        msgId: data[i].msgId
                    });
                }
            }
        });
    });
</script>
<style>
    <% include ../style/reset.css %>
</style>
<style>
    <% include ../style/common.css %>
</style>